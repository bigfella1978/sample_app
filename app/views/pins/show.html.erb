<h1>More Details...</h1>

<%= image_tag @pin.image.url, width: '400'%> 

<p>
  <strong>Description:</strong>
  <%= @pin.description %>
  <p><strong><%= @pin.user.name if @pin.user %></strong></p>
</p>

<% if @pin.user == current_user %>
	<%= link_to 'Edit', edit_pin_path(@pin) %>
<% end %>
<%= link_to 'Back', pins_path %>



